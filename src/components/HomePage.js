import React from 'react';
import PagesContainer from '../containers/PagesContainer';

const HomePage = () => {
  return (
    <div>
      <PagesContainer />
    </div>
  );
};

export default HomePage;
